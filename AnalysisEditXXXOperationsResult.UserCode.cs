///////////////////////////////////////////////////////////////////////////////
//
// This file was automatically generated by RANOREX.
// Your custom recording code should go in this file.
// The designer will only add methods to this file, so your custom code won't be overwritten.
// http://www.ranorex.com
// 
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Text;
using System.Text.RegularExpressions;
using System.Drawing;
using System.Threading;
using WinForms = System.Windows.Forms;

using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Testing;

namespace MyAssays.Wizard.RanorexTests
{
    public partial class AnalysisEditXXXOperationsResult
    {
    	Dictionary<System.Windows.Point,Tuple<System.Windows.Point,Boolean>> points;
    	
        /// <summary>
        /// This method gets called right after the recording has been started.
        /// It can be used to execute recording specific initialization code.
        /// </summary>
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        public void ValidateTemplateButtonsNotVisisble()
        {
        	Validate.IsFalse(repo.ResultsAnalysis.NewTemplateInfo.Exists(TimeSpan.FromSeconds(5)) && repo.ResultsAnalysis.NewTemplate.Visible,
        	                "Validate New template button not visible");
        	
        	Validate.IsFalse(repo.ResultsAnalysis.EditTemplateInfo.Exists(TimeSpan.FromSeconds(5)) && repo.ResultsAnalysis.EditTemplate.Visible,
        	                "Validate Edit template button not visible");
        	
        	Validate.IsFalse(repo.ResultsAnalysis.DeleteTemplateInfo.Exists(TimeSpan.FromSeconds(5)) && repo.ResultsAnalysis.DeleteTemplate.Visible,
        	                "Validate Delete template button not visible");
        }

        public void ValidateInsertHeaderFooterNotVisible()
        {
        	Validate.IsFalse(repo.ResultsAnalysis.InsertHeaderFooterButtonInfo.Exists(TimeSpan.FromSeconds(10)) && repo.ResultsAnalysis.InsertHeaderFooterButton.Visible,
        	                "Validate New template button not visible");
        }

        public void ValidatePointFlagState(bool expectedState, string pointX, string pointY)
        {
        	InitializeFlaggablePointsDictionary();
        	var pointValue = points[new System.Windows.Point(Convert.ToDouble(pointX), Convert.ToDouble(pointY))];
        	Validate.AreEqual(pointValue.Item2, expectedState, String.Format("Validate point flag state is {0}", expectedState));
        }

        public void FlagPoint(string pointX, string pointY)
        {
        	InitializeFlaggablePointsDictionary();
        	var pointValue = points[new System.Windows.Point(Convert.ToDouble(pointX), Convert.ToDouble(pointY))];

        	repo.ResultsAnalysis.ChartTopPanel.SelectedTab.Find<Ranorex.WpfElement>(".//element[@automationid='SciChartSurface']/container[@automationid='PART_MainGrid']/element[@automationid='PART_GridLinesArea']")[0].Click(new Location(((int)pointValue.Item1.X), ((int)pointValue.Item1.Y)));
        	Delay.Seconds(3);
        }
        
        public void InitializeFlaggablePointsDictionary()
        {
        	points = ((Dictionary<System.Windows.Point,Tuple<System.Windows.Point,Boolean>>)repo.ResultsAnalysis.ChartTopPanel.SelectedTab.Find<Ranorex.WpfElement>("./container[@type='AnalysisControl']")[0].Element.GetAttributeValue("FlaggablePoints"));			
        }

        public void ValidateFlagsPanelCheckboxNotExist()
        {
        	Report.Log(ReportLevel.Info, "Validation", "Validating NotExists on item 'ResultsAnalysis.FlagPanelChackbox'.", repo.ResultsAnalysis.FlagPanelChackboxInfo, new RecordItemIndex(8));
        	Validate.IsFalse(repo.ResultsAnalysis.FlagPanelChackboxInfo.Exists(TimeSpan.FromSeconds(10)));
        }


    }
}